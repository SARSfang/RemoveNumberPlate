# 全自动车牌消除程序设计方案

> 日期：2026-07-27
> 用户：汽车摄影师
> 目标：拖入图片即可全自动输出 AI 消除车牌后的图片，达到商拍级无痕质量

---

## 一、需求确认

| 维度 | 决策 |
|---|---|
| 使用形式 | 桌面拖拽 App（Windows） |
| 硬件 | NVIDIA 独显可用 |
| 质量取向 | 摄影师级无痕修复（商拍可交付） |
| 车牌范围 | 仅中国车牌（蓝/绿/黄底） |
| 多车牌处理 | 全自动全部消除 |
| 输出方式 | 保存到同名 `output/` 子文件夹，保留 EXIF 与原文件名 |
| 异常适配 | **必须**支持识别错误/漏检/误检的人工补救 |

---

## 二、技术选型

### 检测：YOLOv8n/s 中国车牌专用模型
- **来源**：社区已有开源预训练权重（如 `yolov8n-plate.pt`，参见 GitHub `PlateReco-ImgRemover`、`morsetechlab/Yolov11-License-Plate-Detection` 等）
- **理由**：精度 95%+，单图检测 < 100ms；可后续用自拍数据微调
- **置信度阈值**：默认 0.4，可在设置中调整（0.2-0.8）

### 修复：LaMa (Large Mask Inpainting)
- **来源**：WACV 2022 论文官方实现，使用 `simple-lama-inpainting` 一行调用版
- **理由**：基于 FFC（傅里叶卷积），擅长大区域周期性纹理修复（车漆、进气格栅、保险杠），对原图风格零破坏
- **性能**：NVIDIA GPU 1-3 秒/图（4K 图）
- **模型大小**：约 200MB

### GUI：PySide6
- **理由**：Qt 官方 Python 绑定，LGPL 协议商用友好；原生拖拽支持；可打包为 exe
- **打包**：PyInstaller → 单个 `消除车牌.exe`

### 图像处理：OpenCV + Pillow + piexif
- OpenCV：高效读写
- piexif：完整保留 EXIF（摄影师必需，含相机型号/拍摄参数/GPS 等）

---

## 三、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    GUI 层 (PySide6)                          │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ 拖拽输入区   │  │ 预览/编辑画布  │  │  设置/进度面板    │   │
│  └──────┬──────┘  └──────┬───────┘  └────────┬─────────┘   │
└─────────┼────────────────┼───────────────────┼──────────────┘
          │ 图片列表         │ 框选/笔刷编辑      │ 参数
          ▼                  ▼                   ▼
┌─────────────────────────────────────────────────────────────┐
│                 核心处理管线 (Pipeline)                       │
│  ┌──────────┐  ┌──────────────┐  ┌──────────┐  ┌────────┐  │
│  │ 加载+EXIF │→│ YOLO 车牌检测 │→│ 掩码生成  │→│ LaMa修复│  │
│  └──────────┘  └──────────────┘  └──────────┘  └────┬───┘  │
│                                                     │      │
│  ┌──────────────────────────────────────────────────▼────┐ │
│  │   输出层：保存到 output/ 子文件夹，保留 EXIF/原文件名    │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
          ▲
          │ 人工补救（漏检/误检时）
┌─────────┴───────────────────────────────────────────────────┐
│              异常适配机制 (Fallback)                          │
│  - 漏检：用户在预览画布上手绘笔刷掩码 → 重跑 LaMa             │
│  - 误检：用户点击框线删除 → 不修复该区域                    │
│  - 低置信度：黄色高亮显示，需用户确认后再处理                  │
│  - 修复失败：保留原图，记录到错误日志                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 四、核心工作流（用户视角）

1. **启动程序** → 看到"拖图片到这里"的空旷拖拽区
2. **拖入图片**（支持多选/整个文件夹；v1 支持 jpg/jpeg/png/tiff/bmp，RAW 格式 dng/arw/cr2 等列入未来扩展）
3. **自动检测阶段**：程序对每张图跑 YOLO，按置信度分色显示：
   - 🟢 绿框：置信度 ≥ 0.6（高置信，自动确认）
   - 🟡 黄框：0.4 ≤ 置信度 < 0.6（低置信，待确认）
   - 🔴 红标：未检测到车牌（需手动处理）
4. **预览校对阶段**（关键适配环节）：
   - 用户可点击任意框删除（误检）
   - 用户可在红标图上手绘笔刷标记漏检车牌
   - 一键"全部确认"跳过校对
5. **点击"开始处理"** → 进度条显示，后台批量跑 LaMa
6. **完成**：弹出输出文件夹，每张图旁生成"原图名_output.jpg"
7. **事后核查**：可选开启"同时生成检测标注图"模式，便于事后翻看哪些被消除了

---

## 五、目录结构

```
消除车牌/
├── app/
│   ├── __init__.py
│   ├── main.py                  # 程序入口
│   ├── config.py                # 默认配置（阈值、路径、模型路径）
│   ├── gui/
│   │   ├── __init__.py
│   │   ├── main_window.py       # 主窗口 + 拖拽事件
│   │   ├── drop_zone.py         # 拖拽输入区组件
│   │   ├── preview_canvas.py    # 预览画布（显示框 + 笔刷编辑）
│   │   ├── progress_panel.py    # 进度/日志面板
│   │   └── settings_dialog.py   # 设置对话框
│   ├── core/
│   │   ├── __init__.py
│   │   ├── detector.py          # PlateDetector: YOLO 封装
│   │   ├── inpainter.py         # Inpainter: LaMa 封装
│   │   ├── mask_utils.py        # 检测框→掩码（含 padding/边缘羽化）
│   │   ├── pipeline.py          # BatchPipeline: 批量调度
│   │   ├── exif_utils.py        # EXIF 完整保留
│   │   └── image_io.py          # 多格式读写
│   └── utils/
│       ├── __init__.py
│       ├── file_utils.py        # 文件名/路径处理
│       └── logger.py            # 日志
├── models/
│   ├── yolov8_plate.pt          # 中国车牌检测权重（启动时若缺失自动下载）
│   └── lama/
│       └── big-lama.pt          # LaMa 修复权重
├── assets/
│   └── icons/                   # 应用图标
├── output/                      # 默认输出目录（运行时生成）
├── logs/                        # 错误日志（运行时生成）
├── requirements.txt
├── run.py                       # 启动脚本
└── README.md                    # 用户使用说明
```

---

## 六、关键模块设计

### 6.1 PlateDetector (`app/core/detector.py`)
```python
class PlateDetector:
    def __init__(self, model_path, device='cuda', conf=0.4):
        ...
    def detect(self, image_bgr) -> List[Detection]:
        """返回 [{bbox: (x1,y1,x2,y2), conf: float}, ...]"""
```
- 首次运行自动从配置 URL 下载权重到 `models/`
- 支持 GPU/CPU 自动切换
- 输入 BGR numpy 数组，输出检测列表

### 6.2 Inpainter (`app/core/inpainter.py`)
```python
class Inpainter:
    def __init__(self, model_path, device='cuda'):
        ...
    def inpaint(self, image_rgb, mask) -> np.ndarray:
        """image_rgb + mask(0/255) → 修复后的 RGB"""
```
- 基于 `simple-lama-inpainting`（pip 包）
- 掩码为二值图，255=需修复区域

### 6.3 MaskUtils (`app/core/mask_utils.py`)
- `box_to_mask(bbox, shape, padding=10, feather=True)`: 检测框转掩码
  - 向外 padding 10px 确保覆盖车牌边缘
  - 边缘羽化（高斯模糊）避免修复边界明显
- `merge_masks(masks)`: 多车牌掩码合并
- `brush_to_mask(brush_strokes)`: 用户笔刷转掩码

### 6.4 BatchPipeline (`app/core/pipeline.py`)
- `QThread` 子类，避免阻塞 GUI
- 信号：`progress_updated(int)`, `image_done(str)`, `error_occurred(str, str)`
- 流程：load → detect → mask → inpaint → save
- 失败图片不中断整体流程，记入日志

### 6.5 PreviewCanvas (`app/gui/preview_canvas.py`)
- 继承 `QGraphicsView`
- 支持显示检测框（可点击删除）
- 支持笔刷模式（可调粗细）绘制掩码
- 缩放/平移支持大图

### 6.6 EXIF 保留 (`app/core/exif_utils.py`)
- 使用 `piexif` 完整保留 EXIF
- 输出格式：与原格式一致（jpg→jpg, png→png）
- 若原格式不支持 EXIF（如 bmp），降级为保存为 jpg 并警告

---

## 七、异常适配机制（关键需求）

| 异常场景 | 处理策略 |
|---|---|
| **未检测到车牌** | 图片标记为"待手动处理"，在预览画布上用户可手绘笔刷掩码，加入处理队列 |
| **低置信度检测** (0.4-0.6) | 黄框显示，默认不处理，需用户点击确认后才纳入修复 |
| **误检**（框错区域） | 用户点击框线删除，该区域不修复 |
| **漏检**（检测漏了某车牌） | 用户在预览画布手绘笔刷补充掩码 |
| **修复失败**（LaMa 报错/显存不足） | 保留原图，记入 `logs/error.log`，最后弹窗汇总 |
| **图片损坏/格式不支持** | 跳过，记入日志，提示用户 |
| **GPU 显存不足** | 自动降级为 CPU 模式并提示用户处理速度变慢 |

---

## 八、性能预估

| 阶段 | NVIDIA GPU (RTX 3060+) | CPU |
|---|---|---|
| YOLO 检测 | ~50ms | ~500ms |
| LaMa 修复 (4K) | ~2s | ~30s |
| 端到端单图 | ~2.5s | ~31s |
| 100 张批量 | ~5 分钟 | ~50 分钟 |

---

## 九、依赖清单 (requirements.txt)

```
PySide6>=6.6
ultralytics>=8.2.0          # YOLOv8
simple-lama-inpainting>=0.1 # LaMa 一行调用版
opencv-python>=4.9
Pillow>=10.0
piexif>=1.1.3               # EXIF 完整保留
numpy>=1.24
torch>=2.0                  # 自动选 CUDA 版本
torchvision
```

---

## 十、实施步骤

1. **项目初始化**：创建目录结构、`requirements.txt`、`run.py` 启动脚本
2. **核心模块**：实现 `detector.py`、`inpainter.py`、`mask_utils.py`、`exif_utils.py`，各自带单元自测
3. **管线集成**：`pipeline.py` 串起全流程，用 5-10 张测试图验证端到端
4. **GUI 基础**：`main_window.py` + `drop_zone.py`，能拖入图片并显示
5. **预览/编辑画布**：`preview_canvas.py`，支持检测框显示/删除/笔刷
6. **进度面板 + 设置对话框**：完善交互
7. **异常适配机制**：实现所有第七节列出的场景
8. **模型权重自动下载**：首次启动体验优化
9. **测试**：用 50+ 张实拍图（含多车牌、逆光、模糊、夜间）验证
10. **打包**：PyInstaller 打包为 `消除车牌.exe`，附带 `models/` 目录

---

## 十一、未来可扩展（不在 v1 范围）

- 视频批量处理
- RAW 格式（dng/arw/cr2 等）原生支持（v1 用户需先用 LR/PS 转出 tiff/jpg）
- 训练自有 YOLO 模型（用自拍数据微调，进一步提升精度）
- 处理港澳台/国际车牌
- 修复结果二次精修（接入 SD inpainting 作为可选高级模式）
- Mac 版本（M 系列芯片用 MPS 后端）

---

## 十二、验收标准

- ✅ 拖入 50 张含中国车牌的汽车摄影图，全部输出无车牌图
- ✅ 修复区域肉眼不可见痕迹（车漆/格栅/保险杠纹理自然延续）
- ✅ EXIF 完整保留（用 exiftool 比对前后无丢失）
- ✅ 处理速度：NVIDIA GPU 上单图 ≤ 3 秒
- ✅ 漏检/误检可通过预览画布手动补救
- ✅ 打包为单个 exe，双击即可运行（无需 Python 环境）
