# 发布说明

## v1.0.0（首个正式版）

**作者**：Fang Dian（方典）<fangdian@tju.edu.cn>
**发布日期**：2026-07-30
**许可**：本地离线使用

### 下载与校验

- 安装包：`消除车牌-Setup-v1.0.0-win64.exe`
- SHA-256：`7aba7c2f322a268cfe7de1a921b7f1e57b57a6bc27cd4c21369e1b8fb8dae1d8`
- 免安装测试版：`dist\preview\1.0.0\消除车牌\消除车牌.exe`（双击 `启动测试版.cmd` 即可运行）

### v1.0.0 重点改进（相对 v0.3.0 开发版）

- **配色系统升级**：暗色主题底色从蓝黑 `#070b12` 改为深灰 `#0a0a0a`（更柔和、不刺眼）；所有底色/文字/边框从冷蓝调改为纯灰阶；状态色统一为 Tailwind 标准色值。
- **白天主题（跟随系统）**：新增纯白系白天主题，通过 CSS `prefers-color-scheme` 媒体查询自动跟随系统主题切换，零配置；白天底色 `#ffffff` + 深蓝 accent `#2563eb`，满足 WCAG AA 对比度。
- **关于对话框作者信息**：关于对话框显示作者 `Fang Dian（方典）`、邮箱 `fangdian@tju.edu.cn`、版权 `© 2026 Fang Dian（方典）. 保留所有权利。`。
- 完整功能继承自 v0.3.0 开发版（监视文件夹、批量后处理、项目预设、历史搜索、图片水印、关于对话框）。

### 主题切换使用方式

应用自动跟随 Windows 系统主题：
1. 打开 **Windows 设置** → **个性化** → **颜色**
2. 选择"选择模式"为"深色"或"浅色"
3. 应用立即切换主题，无需重启

### 关于对话框

点击应用顶部右上角的版本号按钮 → 弹出"关于 消除车牌"对话框，可查看：
- 软件名称与版本号
- 作者：Fang Dian（方典）
- 邮箱：fangdian@tju.edu.cn
- AI 引擎与数据库 schema 版本
- 版权声明

## v0.3.0（开发版）

本版本为主要开发迭代版本，新增监视文件夹功能。

- 监视文件夹：在设置页登记本地文件夹后，新增或修改的照片（JPEG/PNG/TIFF）会自动入队处理，无需手动拖入。
- 启动扫描：应用启动时自动扫描已登记的监视文件夹，发现未处理的照片自动入队；可在 UI 取消，已收集的照片仍会处理。
- 取消语义：取消当前批处理时，会同时清空监视队列中的待处理条目，当前正在处理的单张照片会安全完成。
- 网络驱动器不支持：UNC 路径和映射网络盘在添加和启动扫描时均被拒绝。
- 数据库 schema 升级到 v5，新增 `file_mtime` 和 `file_size` 列；旧库自动迁移，保持兼容。
- 性能预算：文件稳定后入队延迟 ≤ 2 秒，启动扫描 ≤ 3 秒（典型规模）。
- 批量后处理：处理完成后可自动重命名、叠加水印、填写 EXIF（Artist、Copyright、ImageDescription）；在设置页“批量后处理”区块配置，默认关闭，需显式启用；后处理失败不阻塞批处理，保留原消除结果。
- 命名模板支持 `{original}`、`{ext}`、`{seq:03}`、`{client}`、`{date}` 占位符；水印支持文字（字号、颜色）与图片两种类型，可切换；图片水印支持本地 PNG/JPG/BMP/TIFF 选择、缩放比例（相对原图短边 5%–100%）和 9 宫格位置；透明度（0%–100%）共用。
- 关于对话框：顶部栏点击版本号按钮可查看软件版本、AI 运行时、数据库 schema 版本和版权信息；纯本地展示，无网络请求。
- 项目预设：按客户/项目保存完整预设（处理参数 + 后处理配置 + 输出目录规则），批处理页顶部“当前项目”选择器一键套用；项目管理对话框支持 CRUD。
- 输出目录三种模式：源文件旁、项目子文件夹、固定目录；删除项目时任务保留（ON DELETE SET NULL），旧任务无 `project_id` 正常显示。
- 历史搜索：任务历史支持文件名模糊搜索、按状态/日期/项目筛选和分组；前端 ComparisonView 组件实现原图与结果并排对比，滑块拖拽，`← / →` 移动滑块，双击全屏。
- 数据库 schema 升级到 v7（含 `post_processed_output` 列、`projects` 表和 `project_id` 外键）；旧库自动迁移，保持兼容。
- 性能预算：单图后处理增量 ≤ 500ms，500 条任务历史搜索 ≤ 200ms。

## v0.2.0-rc.7（RC7）

这是面向 Windows 10/11 x64 的本地候选测试版。应用完全离线，不需要自行训练、
Python、CUDA、云端接口或付费服务。

### RC7 重点改进

- 每张非处理中照片都可从主预览进入“调整区域”，不再只依赖自动风险复核；
- 检测器、数据库和编辑器端到端保留四点透视轮廓，不再把斜拍车牌强制变成大矩形；
- 默认消除边缘改为沿车牌轮廓向外 35%，可在设置中永久保存，并可在 -30% 到 +100% 间按 1% 调整；
- 支持独立拖动四个角、整体移动、新增/删除车牌框、画笔、橡皮擦和撤销/重做；
- 先生成本地临时结果，满意后才保存，不满意可以继续调整；
- 保存复用临时结果，不重复运行 LaMa，并自动追加 `_clean_2`、`_clean_3`；
- 原图、旧结果、检测四点和调整历史均保留；
- 完成上线前界面收口：统一 40px 操作目标、滑块样式、禁用状态和辅助说明；
- 修复 Windows 高缩放下启动页与复核操作被裁切的问题，复核工具栏会自动重排；
- 历史任务在窄窗口中使用可关闭详情抽屉，支持 Escape 关闭并恢复键盘焦点；
- 新增固定 `dist\preview` 免安装测试版和 `启动测试版.cmd`，日常测试无需反复安装；
- 预览构建采用版本化目录和当前版本指针，旧目录被 Windows 占用时也能继续生成新版；
- 数据库模式从 3 自动迁移到 4，旧矩形检测记录继续兼容。

### 当前测试方式

1. 关闭已经运行的软件。
2. 运行 `packaging\build_preview.ps1`。
3. 双击根目录 `启动测试版.cmd`。
4. 用代表性实拍验证自动四点、边缘范围、临时预览和版本化保存。

正式安装程序将在免安装版完成摄影师样片验收后再构建，避免每轮测试都安装和卸载。

### 已知限制

- 仅支持 JPEG、PNG、TIFF；不直接读取 CR3、NEF、ARW 等 RAW；
- 四点区域解决遮罩过大的主要问题，但极复杂反光或遮挡仍可能需要画笔修正；
- 候选版未使用商业代码签名证书，Windows 可能显示信誉提示；
- 正式商业发行仍需要更大规模私有商拍样片验收、权重条款法律复核和代码签名。
