# 发布说明

## v0.2.0-rc.5（RC5）

这是面向 Windows 10/11 x64 的候选发布版。应用完全离线处理照片，不要求安装
Python、CUDA 或独立显卡，也不使用云端接口或付费服务。

### 安装

运行 `消除车牌-Setup-v0.2.0-rc.5-win64.exe`。安装程序按需安装微软官方
WebView2 Evergreen Runtime，并创建开始菜单项和卸载入口；桌面快捷方式为可选项。

### RC5 重点改进

- 批处理页升级为“单张大图 + 底部胶片带”的摄影工作台；
- 原图和处理结果使用分栏切换，不同时显示；
- 支持自动跟随当前处理项、固定预览和一键恢复跟随；
- 新增安全的任务缩略图、主预览和打开输出接口，不向前端暴露文件路径；
- 胶片带超过 40 项自动窗口化，缩略图缓存上限 64、主预览缓存上限 6；
- 右侧详情在窄窗口折叠为抽屉，1040×680 和 200% 缩放下保留核心操作；
- 待复核、任务历史和设置统一为同一套高端克制的深色视觉系统；
- 前端按状态、桥接、预览、胶片带、复核、历史和设置拆分，仍保持无框架、无打包器；
- 保留 RC4 的任务恢复、诊断导出、模型完整性校验和原片永不覆盖策略。

### 已知限制

- 仅支持 JPEG、PNG、TIFF；本版不直接读取 CR3、NEF、ARW 等 RAW；
- 自动结果仍建议在复杂反光、贴边、遮挡和强透视场景中人工复核；
- 首次运行需要系统具备或安装 WebView2 Evergreen Runtime；
- 候选版未使用商业代码签名证书，Windows 可能显示信誉提示。

本候选版继续用于本地功能与摄影师样片验收，不应在商业授权、代码签名和真实私有
样片回归尚未完成时宣称为正式商业发行版。
